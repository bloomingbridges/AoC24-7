export function ADD( a, b ) {
    return a + b;
};
ADD.operation = "add";
ADD.symbol = '+';

export function MULTIPLY( a, b ) {
    return a * b;
};
MULTIPLY.operation = "multiply";
MULTIPLY.symbol = '*';